# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# Rust build artifacts
/target/

# Local product specifications (must never be committed)
/specs/

# Private hardware research captures (publish only reviewed fixtures)
/traces/

# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml
